<header>
    <nav>
        <% if (req.session.user == undefined) { %>
            <h1 class="logo">Ecommerce</h1>
            <div class="nav-links">
                <a href="/">Home</a>
                <a href="">Contact</a>
                <a href="">About</a>
                <a href="/users/login">Sign In</a>
            </div>
            <% } else { %>
                <h1 class="logo">Ecommerce</h1>
                <div class="nav-links">
                    <a href="/">Home</a>
                    <a href="">Contact</a>
                    <a href="">About</a>
                    <a href="/users/<%= req.session.user.id %>">Profile</a>
                    <a href="">Cart</a>
                    <% if (req.session.user.op == 1) { %>
                        <a href="">Admin</a>
                        <% } %>
                </div>
                <% } %>
                    <div class="search-form">
                        <form>
                            <label for="search-bar">
                    <input type="search" name="search-bar" placeholder="Search..."/>
                </label>
                            <button type="submit">Search</button>
                        </form>
                    </div>
                    <div class="burger">
                        <div class="level1"></div>
                        <div class="level"></div>
                        <div class="level1"></div>
                    </div>
    </nav>
</header>