userVerifiedMiddlewere = {

    //check if users is verified,
    //if verified, next();
    //else send message 'your account is not verified, check your inbox'

}

module.exports = userVerifiedMiddlewere;